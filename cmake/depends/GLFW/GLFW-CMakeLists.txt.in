cmake_minimum_required(VERSION 2.8.2)
 
project(GLFW-download NONE)
 
include(ExternalProject)

ExternalProject_Add(
    GLFW

    GIT_REPOSITORY    "https://github.com/glfw/glfw"
    GIT_TAG           "3.2"

    SOURCE_DIR        "${CMAKE_BINARY_DIR}/3rdParty/GLFW-src"
    BINARY_DIR        "${CMAKE_BINARY_DIR}/3rdParty/GLFW-build"

    CONFIGURE_COMMAND ""
    BUILD_COMMAND     ""
    INSTALL_COMMAND   ""
    TEST_COMMAND      ""
)
